import { permissionModel, roleModel } from "../models/index";

// const admin = await roleModel.create({ name: "admin" });
// const perm1 = await permissionModel.create({ name: "create" });
// await admin.addPermission(perm1);

// const user = await UserTable.create({ name: "Ak", email: "test@example.com" });
// await user.addRole(admin);

// const roles = await user.getRoles();
// console.log(
//   "User Roles:",
//   roles.map((r) => r.name)
// );

// const perms = await admin.getPermissions();
// console.log(
//   "Admin Permissions:",
//   perms.map((p) => p.name)
// );

